# TikTok 风格刷视频网页

一个纯前端的 TikTok-like Demo，支持：

- 全屏竖版视频流
- 鼠标滚轮 / 手机上下滑动切换视频
- 当前视频自动播放，其他视频自动暂停
- 点赞计数

## 本地运行

直接用浏览器打开 `index.html` 即可。

如果你装了 Python，也可以运行：

```bash
python3 -m http.server 8080
```

然后访问 `http://localhost:8080`。
